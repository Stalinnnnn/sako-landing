This folder is no longer needed by the landing page.

The page now uses inline SVG illustrations baked into index.html — no
external images are loaded. The hero visual, the app mockup, and the
three report thumbnails are all SVG inside the HTML.

You can safely delete this folder if you want a cleaner project.
The old hero.png (if still here) is unused.
